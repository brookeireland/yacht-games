{
  "name": "server",
  "private": true,
  "scripts": {
    "dev": "tsx watch --clear-screen=false --experimental-sqlite server ",
    "initdb": "tsx --experimental-sqlite ./init-db",
    "listdb": "tsx --experimental-sqlite ./list-db",
    "types": "tsc -b --watch --preserveWatchOutput"
  },
  "dependencies": {
    "fastify": "^5.3.2",
    "pino-pretty": "^13.0.0"
  },
  "devDependencies": {
    "tsx": "^4.19.3",
    "typescript": "~5.7.3"
  }
}
